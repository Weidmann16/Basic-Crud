@model IEnumerable<PruebaCodere.Grupo>

@{
    ViewBag.Title = "Inicio";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Inicio</h2>

<p>
    @Html.ActionLink("Crear Nuevo", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Nombre_Oficina)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Nombre_Oficina)
        </td>
        <td>
            @Html.ActionLink("Editar", "Edit", new { id=item.Codigo_Area }) |
            @Html.ActionLink("Detalles", "Details", new { id=item.Codigo_Area }) |
            @Html.ActionLink("Eliminar", "Delete", new { id=item.Codigo_Area })
        </td>
    </tr>
}

</table>
